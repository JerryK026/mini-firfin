spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        use_sql_comments: true
    open-in-view: false
    defer-datasource-initialization: true
  # 과제 실행 단순화를 위해 추가함
  sql:
    init:
      mode: always